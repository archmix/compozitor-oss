<?xml version="1.0" encoding="UTF-8"?>
<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>

	<groupId>compozitor</groupId>
	<artifactId>compozitor-oss</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<properties>
		<compozitorVersion>${project.version}</compozitorVersion>
		<javaVersion>1.8</javaVersion>
		<velocityVersion>1.7</velocityVersion>
		<autoServiceVersion>1.0-rc5</autoServiceVersion>
		<lombokVersion>1.18.6</lombokVersion>
		<junitVersion>4.11</junitVersion>
		<mockitoVersion>1.10.8</mockitoVersion>
		<awsVersion>1.11.594</awsVersion>
		<compileTestingVersion>0.18</compileTestingVersion>

		<maven.failsafe.pluginVersion>2.18.1</maven.failsafe.pluginVersion>
		<maven.javadoc.pluginVersion>2.10.1</maven.javadoc.pluginVersion>
		<maven.surefire.pluginVersion>3.0.0-M3</maven.surefire.pluginVersion>
		<maven.compiler.pluginVersion>3.2</maven.compiler.pluginVersion>
		<maven.resources.pluginVersion>2.7</maven.resources.pluginVersion>
		<maven.dependency.pluginVersion>2.10</maven.dependency.pluginVersion>
	</properties>

	<modules>
		<module>compozitor-engine</module>
		<module>compozitor-generator</module>
		<module>compozitor-processor</module>
		<module>compozitor-template</module>
	</modules>

	<dependencyManagement>
		<dependencies>
			<!-- COMPOZITOR -->
			<dependency>
				<artifactId>compozitor-processor</artifactId>
				<groupId>compozitor</groupId>
				<version>${compozitorVersion}</version>
				<optional>true</optional>
			</dependency>
			<dependency>
        <artifactId>compozitor-processor</artifactId>
        <groupId>compozitor</groupId>
        <version>${compozitorVersion}</version>
        <type>test-jar</type>
        <optional>true</optional>
      </dependency>
			<dependency>
				<artifactId>compozitor-engine</artifactId>
				<groupId>compozitor</groupId>
				<version>${compozitorVersion}</version>
				<optional>true</optional>
			</dependency>
			<dependency>
				<artifactId>compozitor-generator</artifactId>
				<groupId>compozitor</groupId>
				<version>${compozitorVersion}</version>
				<optional>true</optional>
			</dependency>
			<dependency>
				<artifactId>compozitor-template</artifactId>
				<groupId>compozitor</groupId>
				<version>${compozitorVersion}</version>
				<optional>true</optional>
			</dependency>

			<!-- LOMBOK -->
			<dependency>
				<groupId>org.projectlombok</groupId>
				<artifactId>lombok</artifactId>
				<version>${lombokVersion}</version>
				<optional>true</optional>
			</dependency>

			<!-- VELOCITY -->
			<dependency>
				<groupId>org.apache.velocity</groupId>
				<artifactId>velocity</artifactId>
				<version>${velocityVersion}</version>
				<optional>true</optional>
			</dependency>

			<!-- AWS -->
			<dependency>
				<groupId>com.amazonaws</groupId>
				<artifactId>aws-java-sdk-s3</artifactId>
				<version>${awsVersion}</version>
				<optional>true</optional>
			</dependency>

			<!-- GOOGLE -->
			<dependency>
				<groupId>com.google.auto.service</groupId>
				<artifactId>auto-service</artifactId>
				<version>${autoServiceVersion}</version>
				<optional>true</optional>
			</dependency>
			<dependency>
				<groupId>com.google.testing.compile</groupId>
				<artifactId>compile-testing</artifactId>
				<version>${compileTestingVersion}</version>
				<optional>true</optional>
			</dependency>

			<!-- TEST -->
			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-core</artifactId>
				<version>${mockitoVersion}</version>
				<optional>true</optional>
			</dependency>
			<dependency>
				<groupId>junit</groupId>
				<artifactId>junit</artifactId>
				<version>${junitVersion}</version>
				<optional>true</optional>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven.compiler.pluginVersion}</version>
					<configuration>
						<source>${javaVersion}</source>
						<target>${javaVersion}</target>
						<compilerArgs>
							<arg>-Acodetrans.output=${project.basedir}/src/main</arg>
						</compilerArgs>
					</configuration>
				</plugin>

				<plugin>
					<artifactId>maven-resources-plugin</artifactId>
					<version>${maven.resources.pluginVersion}</version>
				</plugin>

				<plugin>
					<artifactId>maven-dependency-plugin</artifactId>
					<version>${maven.dependency.pluginVersion}</version>
				</plugin>

				<plugin>
					<artifactId>maven-source-plugin</artifactId>
					<executions>
						<execution>
							<id>attach-sources</id>
							<goals>
								<goal>jar</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<plugin>
					<artifactId>maven-javadoc-plugin</artifactId>
					<executions>
						<execution>
							<id>attach-javadocs</id>
							<goals>
								<goal>jar</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<plugin>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>${maven.surefire.pluginVersion}</version>
				</plugin>

				<plugin>
					<artifactId>maven-failsafe-plugin</artifactId>
					<version>${maven.failsafe.pluginVersion}</version>
					<executions>
						<execution>
							<goals>
								<goal>integration-test</goal>
								<goal>verify</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-release-plugin</artifactId>
					<version>2.5.3</version>
					<configuration>
						<tagNameFormat>v@{project.version}</tagNameFormat>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<scm>
		<connection>scm:git:ssh://github.com/mrbraztech/compozitor-oss.git</connection>
		<developerConnection>scm:git:ssh://git@github.com/mrbraztech/compozitor-oss.git</developerConnection>
		<url>https://github.com/mrbraztech/compozitor-oss</url>
	</scm>

	<distributionManagement>
		<repository>
			<id>compozitor-oss</id>
			<name>compozitor-oss</name>
			<url>file://${project.basedir}/maven-repo</url>
		</repository>
		<snapshotRepository>
			<id>compozitor-oss</id>
			<name>compozitor-oss</name>
			<url>file://${project.basedir}/maven-repo</url>
		</snapshotRepository>
	</distributionManagement>
</project>
